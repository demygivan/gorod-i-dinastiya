# Tile art (future)

Placeholder tiles are generated in code by `world/city_placeholder_tileset.gd`
until atlas images are added here.

Expected sources:
- `ground.png`, `road.png`, `bridge.png`, `water.png`, `plaza.png` (32×32)
