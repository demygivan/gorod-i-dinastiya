class_name Enums
extends RefCounted
## Общие перечисления проекта. Не хранит состояние — только константы.

enum Profession { PATRON, CRAFTSMAN, SCHOLAR, ROGUE }

enum BuildingFunction { PRODUCTION, SERVICE, ACTION }

enum BuildingType {
	FARM, BAKERY, MINE, SMITHERY, TAILOR, TAVERN, HOSPITAL, TEMPLE
}
